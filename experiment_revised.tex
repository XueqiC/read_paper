\section{Experimental Setting}
\label{subsec:exp_setup}

\textbf{Models and budgets.}
We mainly distill Llama-3.3-70B-Instruct into Llama-3.1-8B-Instruct, and report
Qwen2.5-72B-Instruct to Qwen2.5-14B-Instruct results in
Appendix~\ref{sec:app_experiment}. All methods use the same student checkpoint,
training recipe, and $20$M/$150$M distillation-token budgets.

\textbf{Evaluation protocol.}
We evaluate eight capabilities using Appendix
Table~\ref{tab:capabilities-benchmarks}. For each benchmark, 10\% of non-test
data is used for profiling, requirement inference, and allocation updates; the
held-out split is reserved for final reporting. Generation uses temperature $0$
with the same prompt template across methods. We report mean $\pm$ standard
deviation over three seeds.

\textbf{Baselines.}
For each capability, we build a held-out-filtered distillation pool from its
benchmark prompts. For multi-dataset capabilities, we use balanced dataset-level
sampling. The six main baselines combine three teacher signals---final response
(Resp), chain-of-thought plus response (CoT), and token-level logits (Logit)---
with two objectives, SFT and KD. Each baseline is one-hot at the capability
level, spending the full budget on the target-capability pool.
\rev{We also compare against four allocation baselines that share ReAD's
generator, teacher, and budget and differ only in how $\mathbf{w}_t$ is chosen
(Figure~\ref{fig:diagnostics}c): \emph{Uniform static}
($\mathbf{w}_t=\mathbf{1}/|\mathcal{C}|$), \emph{Task-static mix}
($\mathbf{w}_t=\mathbf{r}_\tau$), \emph{Greedy one-step} (re-selects
$\mathbf{w}_t$ each step by the ensemble mean alone, $\kappa=0$), and
\emph{Grid-searched} (best fixed allocation in $\mathcal{A}(\tau)$ on the
profiling split). These are the matched reference points for adaptive
allocation. Finally, we include three published capability-specific
distillation methods, adapted to our teacher--student pair and budget:
step-by-step distillation~\citep{hsieh2023stepbystep} for reasoning,
Equation-of-Thought Distillation (EoTD)~\citep{zhu2024distilling} for math,
and CodePLAN~\citep{sun2024enhancing} for code.}

\textbf{ReAD implementation.}
ReAD runs for $20$ decision steps, with bandit updates, proxy refreshes, and data
resampling every $1.0$M tokens in the $20$M setting and every $7.5$M tokens in
the $150$M setting. Proxy refresh adds 3.12\% wall-clock overhead, the full online loop adds
5.63\%, and the reusable requirement identifier costs 3.79 GPU-hours to train
once.
\rev{\textbf{Cost accounting.} $B$ counts the teacher-generated tokens the
student is trained on and is identical for all methods. Two ReAD-specific costs
fall outside $B$ and are reported separately rather than folded into it: the
identifier is trained once from auxiliary tasks and reused for every task and
budget, so its 3.79 GPU-hours are amortized, and the fixed probe suite is the
3.12\% overhead above.}

\section{Experiments}
\label{sec:exp}

We study four questions: \textbf{RQ1} utility gains under the same budget\rev{,
including against capability-specific distillation methods};
\textbf{RQ2} reduced waste and spillover; \textbf{RQ3} the value of adaptive
bandit scheduling over static or greedy alternatives; and \textbf{RQ4}
transfer beyond capability-aligned benchmarks\rev{. Figure~\ref{fig:signif}
gives a statistical view of every comparison.}

\begin{table*}[t]
\centering
\tiny
\setlength\tabcolsep{1pt}
\caption{\rev{Capability profile on Llama under 20M- and 150M-token budgets
(mean $\pm$ std over three seeds). ReAD achieves the strongest profile at both
budgets while using the same student initialization and token budget as all
baselines. Best in bold, second best underlined.}}
\begin{tabular}{lcccccccc}
\toprule
\textbf{Method} &
\textbf{General} &
\textbf{Steer.} &
\textbf{Reason.} &
\textbf{Math} &
\textbf{Code} &
\textbf{Tool} &
\textbf{LCU} &
\textbf{Multi.} \\
\midrule
Teacher only &
$48.13\pm0.35$ & $89.98\pm0.40$ & $10.51\pm0.12$ & $48.34\pm0.55$ &
$88.40\pm0.70$ & $31.90\pm0.45$ & $36.20\pm0.40$ & $91.10\pm0.55$ \\
Student only &
$31.09\pm0.40$ & $49.22\pm0.55$ & $8.72\pm0.15$ & $15.56\pm0.60$ &
$72.60\pm0.95$ & $25.83\pm0.60$ & $30.40\pm0.45$ & $68.90\pm0.80$ \\
\midrule
\multicolumn{9}{@{}l}{\textit{Budget $B=20$M}} \\
Resp-SFT & $33.28\pm0.47$ & $52.84\pm0.71$ & $9.05\pm0.18$ & $17.12\pm0.83$ & $74.06\pm1.09$ & $26.92\pm0.78$ & $31.28\pm0.51$ & $70.02\pm0.93$ \\
CoT-SFT & $33.02\pm0.52$ & $52.16\pm0.76$ & $9.46\pm0.14$ & $18.58\pm0.77$ & $73.82\pm1.02$ & $26.81\pm0.74$ & $31.19\pm0.57$ & $69.78\pm0.88$ \\
Logit-SFT & $33.61\pm0.44$ & $53.04\pm0.69$ & $9.12\pm0.17$ & $17.63\pm0.79$ & $74.48\pm1.15$ & $27.03\pm0.70$ & $31.42\pm0.53$ & $70.21\pm0.90$ \\
Resp-KD & $33.83\pm0.50$ & $53.62\pm0.73$ & $9.18\pm0.16$ & $17.84\pm0.74$ & $74.23\pm1.07$ & $27.18\pm0.76$ & $31.58\pm0.55$ & $70.48\pm0.95$ \\
CoT-KD & $33.46\pm0.46$ & $52.98\pm0.78$ & \underline{$9.62\pm0.13$} & \underline{$19.34\pm0.72$} & $74.03\pm1.10$ & $27.06\pm0.72$ & $31.47\pm0.58$ & $70.08\pm0.89$ \\
Logit-KD & \underline{$34.02\pm0.43$} & \underline{$54.21\pm0.66$} & $9.25\pm0.15$ & $18.20\pm0.76$ & \underline{$74.62\pm1.12$} & \underline{$27.41\pm0.69$} & \underline{$31.72\pm0.50$} & \underline{$70.81\pm0.91$} \\
\cmidrule(lr){1-9}
\textbf{ReAD} &
$\mathbf{35.02\pm0.41}$ & $\mathbf{58.03\pm0.61}$ & $\mathbf{10.01\pm0.12}$ & $\mathbf{21.06\pm0.68}$ &
$\mathbf{77.06\pm0.97}$ & $\mathbf{29.82\pm0.64}$ & $\mathbf{33.58\pm0.49}$ & $\mathbf{73.04\pm0.84}$ \\
\midrule
\multicolumn{9}{@{}l}{\textit{Budget $B=150$M}} \\
Resp-SFT & $38.41\pm0.58$ & $68.73\pm0.94$ & $9.55\pm0.14$ & $28.72\pm1.08$ & $79.18\pm1.26$ & $28.53\pm0.82$ & $32.69\pm0.73$ & $78.23\pm1.12$ \\
CoT-SFT & $38.02\pm0.61$ & $67.92\pm0.89$ & $10.14\pm0.12$ & $31.58\pm0.97$ & $78.86\pm1.33$ & $28.41\pm0.79$ & $32.83\pm0.68$ & $77.77\pm1.06$ \\
Logit-SFT & $39.03\pm0.56$ & $69.38\pm0.86$ & $9.70\pm0.13$ & $29.61\pm1.02$ & $79.69\pm1.21$ & $28.71\pm0.77$ & $33.01\pm0.70$ & $78.63\pm1.09$ \\
Resp-KD & $39.62\pm0.53$ & $70.98\pm0.84$ & $9.82\pm0.12$ & $30.21\pm0.98$ & $80.09\pm1.18$ & $29.09\pm0.74$ & $33.42\pm0.66$ & $79.18\pm1.03$ \\
CoT-KD & $39.12\pm0.55$ & $69.84\pm0.88$ & \underline{$10.26\pm0.11$} & \underline{$33.02\pm0.92$} & $79.81\pm1.24$ & $29.01\pm0.71$ & $33.19\pm0.69$ & $78.71\pm1.01$ \\
Logit-KD & \underline{$40.18\pm0.51$} & \underline{$72.63\pm0.79$} & $9.95\pm0.12$ & $31.36\pm0.95$ & \underline{$80.57\pm1.16$} & \underline{$29.48\pm0.69$} & \underline{$33.73\pm0.64$} & \underline{$79.97\pm0.97$} \\
\cmidrule(lr){1-9}
\textbf{ReAD} &
$\mathbf{41.80\pm0.45}$ & $\mathbf{78.20\pm0.70}$ & $\mathbf{10.40\pm0.10}$ & $\mathbf{35.20\pm0.80}$ &
$\mathbf{83.90\pm1.00}$ & $\mathbf{31.30\pm0.55}$ & $\mathbf{35.60\pm0.55}$ & $\mathbf{84.10\pm0.90}$ \\
\bottomrule
\end{tabular}
\label{tab:llama_20m}
\label{tab:llama_150m}
\end{table*}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figure/rq2_legend.pdf}
  \vspace{-1.5em}

  \begin{subfigure}[t]{0.24\columnwidth}
    \centering
    \includegraphics[width=\linewidth]{figure/rq2_lineplot_reasoning.pdf}
    \caption{Reasoning}
    \label{fig:r2_reason}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.24\columnwidth}
    \centering
    \includegraphics[width=\linewidth]{figure/rq2_lineplot_math.pdf}
    \caption{Math}
    \label{fig:r2_math}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.24\columnwidth}
    \centering
    \includegraphics[width=\linewidth]{figure/rq2_lineplot_tool_use.pdf}
    \caption{Tool Use}
    \label{fig:r2_tool}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.24\columnwidth}
    \centering
    \includegraphics[width=\linewidth]{figure/rq2_lineplot_lcu.pdf}
    \caption{LCU}
    \label{fig:r2_lcu}
  \end{subfigure}

  \caption{Performance versus token budget for four bottleneck capabilities. Curves show mean $\pm$ standard deviation over three seeds.}
  \label{fig:rq2_budget}
  \vspace{-0.8em}
\end{figure}

\subsection{RQ1: Utility under a Fixed Budget}
\label{subsec:exp_results}
\label{subsec:rq1}

Table~\ref{tab:llama_20m} shows that ReAD achieves the strongest capability
profile at $20$M tokens\rev{ and at $150$M tokens, outperforming the six one-hot
baselines on every capability at both budgets}. The gains
are especially pronounced on bottleneck capabilities such as Steerability, Math,
Tool Use, and LCU. \rev{Since each one-hot baseline spends the whole budget on one capability,
this isolates the value of distributing the budget at all; the matched
comparison for \emph{how} to distribute it is Figure~\ref{fig:diagnostics}c (RQ3).} The Qwen results in
Appendix~\ref{sec:app_experiment} show the same ordering, suggesting that the
improvement is not tied to a single teacher--student pair.
\rev{With three seeds, all sixteen differences are positive and all but
Reasoning at $150$M are significant before correction; after Holm correction
over the eight capabilities, Steerability at both budgets and Multilingual at
$150$M remain significant (Figure~\ref{fig:signif}).}

\rev{\textbf{Where the margin comes from.} The margin grows with the
teacher--student headroom (Spearman $\rho=0.74$ at $150$M, $p=0.04$): it is largest on
Steerability and Multilingual (teacher ahead by over $20$ points) and smallest on
Reasoning ($1.8$ points). ReAD's gain over the student is $1.9\times$ that of the
strongest baseline at $20$M and $1.3\times$ at $150$M, so allocation matters most when tokens are scarce.}

\rev{Against the three capability-specific methods at $150$M
(Figure~\ref{fig:radar}), which concentrate gains on their own target as
Section~\ref{sec:emp_res} predicts, ReAD is never worse on that target and clearly
better elsewhere, giving a stronger and more balanced profile.}

\subsection{RQ2: Budget Efficiency and Spillover}
\label{subsec:rq2}

Figure~\ref{fig:rq2_budget} reports budget-scaling curves for Reasoning, Math,
Tool Use, and LCU. ReAD achieves higher performance at the same budget and
maintains the strongest scaling trend, indicating that adaptive allocation
reduces wasted tokens on low-marginal-return updates.
\rev{ReAD also gains the most per additional $10$M tokens on six of the eight
capabilities (Appendix Table~\ref{tab:scaling}). Conversely, interpolating each
baseline log-linearly between its two measured budgets, the strongest one-hot
baseline would need $1.3$--$6.5\times$ ReAD's tokens (median about $2\times$)
to match ReAD's $20$M score, and none reaches ReAD's $20$M Tool Use score even
at $150$M (Table~\ref{tab:equiv_budget}). This token saving is an
order of magnitude larger than ReAD's $5.63\%$ online overhead, so the
advantage does not rest on the costs that we exclude from the token budget $B$ shared
by all methods (Section~\ref{subsec:exp_setup}).}

\input{table_equiv_budget}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figure/diagnostics.pdf}
  \vspace{-1.6em}
  \caption{\rev{Budget-allocation diagnostics (mean $\pm$ std, three seeds; values in
  Appendix Table~\ref{tab:budget_diagnostics}). (a) On-target gain vs.\ off-target
  change and (b) negative transfer at 150M; (c) allocation schedules.}}
  \label{fig:diagnostics}
  \vspace{-0.8em}
\end{figure}

Figure~\ref{fig:diagnostics}a--b shows that ReAD \rev{also} improves the
gain--spillover trade-off. Single-capability KD achieves strong target gains but
causes larger off-target degradation, while static mixing reduces spillover at
the cost of weaker target improvement. ReAD better balances the two: it keeps
strong on-target gains while suppressing negative transfer across
non-target capabilities.

\subsection{RQ3: Adaptive Scheduling}
\label{subsec:rq3}

Figure~\ref{fig:diagnostics}c compares ReAD with stronger allocation
baselines under the same generator and token budgets. Static schedules cannot
adapt once marginal returns and spillover change during training, while the
greedy heuristic ignores uncertainty and remaining budget. ReAD outperforms
both, with a larger margin at $150$M where saturation and spillover are stronger.
\rev{The margins ($+1.0$/$+1.2$ over Greedy one-step and $+1.2$/$+1.4$ over
Grid-searched at $20$M/$150$M; seed std $0.3$--$0.4$) are smaller than over one-hot
baselines because every schedule already mixes capabilities; they measure the value
of re-estimating the mix as the student and the remaining budget change. All four gains remain significant
after Holm correction at $150$M, and the gain over Uniform static also at $20$M
(Figure~\ref{fig:signif}).}

\begin{figure}[t]
  \centering
  \begin{subfigure}[b]{0.44\linewidth}
    \centering
    \includegraphics[height=92pt]{figure/rq3_ablation_avgmetric_20m_150m.pdf}
    \caption{Component ablation}
    \label{fig:rq3_ablation}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.22\linewidth}
    \centering
    \includegraphics[height=92pt]{figure/rqX_radar_sota_vs_read_150m.pdf}
    \caption{Specialized methods}
    \label{fig:radar}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.30\linewidth}
    \centering
    \includegraphics[height=92pt]{figure/xstest_compact.pdf}
    \caption{Held-out XSTest}
    \label{fig:xstest}
  \end{subfigure}
  \vspace{-0.4em}
  \caption{\rev{(a) Component ablation (average score over eight capabilities). (b)
  ReAD vs.\ step-by-step distillation, EoTD, and CodePLAN at 150M (fraction of the
  teacher--student gap closed). (c) XSTest: ReAD (filled) vs.\ the best SFT/KD baseline
  (hollow); blue 20M, orange 150M.}}
  \label{fig:ablation_radar}
  \vspace{-1.0em}
\end{figure}

\rev{\textbf{Component ablation.} Removing the identifier (uniform allocation),
adaptation (fixed allocation), or interaction awareness (no spillover penalty) one at
a time, Figure~\ref{fig:rq3_ablation} shows that identification and adaptation drive
the largest gains and interaction modeling adds a further improvement.}

\subsection{RQ4: Transfer beyond Aligned Benchmarks}
\label{subsec:rq4}

\begin{figure}[t]
  \centering
  \begin{subfigure}[b]{0.37\linewidth}
    \centering
    \includegraphics[height=82pt]{figure/signif_onehot.pdf}
    \caption{vs.\ strongest one-hot baseline}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.33\linewidth}
    \centering
    \includegraphics[height=82pt]{figure/signif_alloc.pdf}
    \caption{vs.\ allocation schedules}
  \end{subfigure}\hfill
  \begin{subfigure}[b]{0.28\linewidth}
    \centering
    \includegraphics[height=82pt]{figure/signif_xstest.pdf}
    \caption{XSTest}
  \end{subfigure}
  \caption{\rev{ReAD's advantage with unadjusted 95\% Welch intervals from the
  reported means and standard deviations (three seeds; blue circles 20M, orange
  squares 150M). Filled markers stay significant after Holm correction per panel
  and budget; safe-refusal is shown as a reduction, so positive values favor ReAD in all panels.}}
  \label{fig:signif}
\end{figure}

\rev{On held-out XSTest~\citep{rottger2024xstest}, with the same pipeline and no new
capability head, ReAD lowers safe-refusal and raises unsafe-refusal scores over the
best SFT/KD baseline at both budgets (Figure~\ref{fig:xstest}, Appendix
Table~\ref{tab:xstest}). The four differences agree in direction, but none survives
Holm correction with three seeds (Figure~\ref{fig:signif}), so this is preliminary
evidence of transfer beyond the eight capabilities rather than a firm result.}

